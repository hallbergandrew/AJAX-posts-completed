<%= form_for post, remote: true do |f| %>
  <%= f.check_box :complete %>
  <%= f.submit "Update" %>
  <%= f.label :complete, post.title %>
  <%= link_to "(remove)", post, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>
